import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { Firearm } from './Firearm.js';
import '../test.css';
const scene=new THREE.Scene();scene.background=new THREE.Color(0x0d1110);scene.fog=new THREE.Fog(0x0d1110,8,22);const camera=new THREE.PerspectiveCamera(42,innerWidth/innerHeight,.1,100);camera.position.set(0,1.15,4.8);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;document.querySelector('#app').prepend(renderer.domElement);const controls=new OrbitControls(camera,renderer.domElement);controls.target.set(0,0,-.55);controls.enableDamping=true;controls.minDistance=2.5;controls.maxDistance=8;scene.add(new THREE.HemisphereLight(0xc4d9ca,0x18231c,2.2));const keyLight=new THREE.DirectionalLight(0xffe5c4,3.4);keyLight.position.set(-4,6,4);scene.add(keyLight);const rimLight=new THREE.PointLight(0xb9ef72,16,9);rimLight.position.set(3,2.8,-2);scene.add(rimLight);const floor=new THREE.Mesh(new THREE.CircleGeometry(5,64),new THREE.MeshStandardMaterial({color:0x17221b,roughness:.86}));floor.rotation.x=-Math.PI/2;floor.receiveShadow=true;scene.add(floor);const ring=new THREE.Mesh(new THREE.RingGeometry(1.5,1.52,64),new THREE.MeshBasicMaterial({color:0x526a58,transparent:true,opacity:.65,side:THREE.DoubleSide}));ring.rotation.x=-Math.PI/2;ring.position.y=.01;scene.add(ring);
const variants=[{name:'Arme 01 · Standard',scale:1,color:0x202825,accent:0x9c7046},{name:'Arme 02 · Compacte',scale:.82,color:0x26352f,accent:0x6f9a82},{name:'Arme 03 · Prototype',scale:1.15,color:0x302b26,accent:0xd29b53}];let activeIndex=0;let activeFirearm;function createVariant(variant){const firearm=new Firearm();firearm.scale.setScalar(variant.scale);firearm.traverse(object=>{if(!object.isMesh)return;object.material=object.material.clone();if(object.geometry.type==='BoxGeometry'&&object.position.y>.1)object.material.color.set(variant.accent);else if(object.position.z<-.9)object.material.color.set(variant.color)});return firearm}function showVariant(index){activeIndex=(index+variants.length)%variants.length;if(activeFirearm)scene.remove(activeFirearm);activeFirearm=createVariant(variants[activeIndex]);scene.add(activeFirearm);document.querySelector('#model-name').textContent=variants[activeIndex].name;document.querySelector('#model-index').textContent=String(activeIndex+1).padStart(2,'0');document.querySelector('#model-total').textContent=String(variants.length).padStart(2,'0')}function resetView(){camera.position.set(0,1.15,4.8);controls.target.set(0,0,-.55);controls.update()}document.querySelector('#previous').addEventListener('click',()=>showVariant(activeIndex-1));document.querySelector('#next').addEventListener('click',()=>showVariant(activeIndex+1));document.querySelector('#reset').addEventListener('click',resetView);addEventListener('keydown',event=>{if(event.code==='ArrowLeft')showVariant(activeIndex-1);if(event.code==='ArrowRight')showVariant(activeIndex+1);if(event.code==='KeyR')resetView()});showVariant(0);function animate(){controls.update();renderer.render(scene,camera)}renderer.setAnimationLoop(animate);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
